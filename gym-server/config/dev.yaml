env: "dev"
storage: "postgres://postgres:root@postgres:5432/health?sslmode=disable" # Если Go код будет выполняться из контейнера, то нужно @localhost:5436/health"
token_ttl: 1h # Срок жизни токена сервиса
auth_token_ttl: 1h # Срок жизни токена аутентификации
grpc:
  port: 9090
  timeout: 15s
http:
  port: 3000 
smtp:
  email_sender: "mysenderdeveloper@gmail.com"
  email_password: "kdbw armw mmrh mlnp"
  username: "mysenderdeveloper"
  host: "smtp.gmail.com"
  port: 587
minio:
  endpoint: "minio:9000"
  access_key: "admin"
  secret_key: "admin123"
  use_ssl: false
  bucket_name: "media"
  location: "us-east-1"  # Регион для bucket  